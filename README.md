# Quarterly Earnings — Reveal.js Presentation

This repo contains a Reveal.js presentation for the quarterly earnings report.

## How to preview locally

1. Clone the repo.
2. Open `index.html` in a browser (or serve with a simple static server).

## Publish

This repo includes a GitHub Actions workflow (`.github/workflows/gh-pages.yml`) to publish the site to GitHub Pages on push to `main`.
